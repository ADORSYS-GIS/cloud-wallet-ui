## Cloud Wallet UI (Vite) environment variables

# Backend host/base URL (used by src/api/client.ts).
# `/api/v1` is appended automatically when missing.
VITE_API_BASE_URL=http://localhost:3000

# When set to `true`, logs API requests/responses and SSE events to the browser console
# (Authorization tokens are redacted). Omit or set to anything else in production builds.
# VITE_DEBUG_API=true

# When set to `true`, presentation API calls (`/presentation/start` and consent)
# are fulfilled with local mock data instead of hitting the backend.
# This lets you demo the frontend presentation flow without a running wallet server.
# VITE_MOCK_PRESENTATION=true

# Optional allowlist for accepting *plain https* QR payloads as credential-offer URLs.
# Comma-separated host[:port] entries (no scheme).
#
# When set, `parseCredentialOfferInput()` only accepts plain https URLs whose `url.host`
# is in this allowlist. `openid-credential-offer://...` links are still accepted normally.
#
# Example:
# VITE_ALLOWED_CREDENTIAL_OFFER_HOSTS=issuer.example.com,wallet.example.org,localhost:8080
# VITE_ALLOWED_CREDENTIAL_OFFER_HOSTS=

